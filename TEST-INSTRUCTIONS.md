# 🎯 테마 카드 클릭 기능 테스트 가이드

## 빠른 시작

### 1️⃣ 테스트 파일 열기

두 가지 방법이 있습니다:

**방법 A: 간단한 테스트 먼저 (권장)**
```
파일 탐색기 열기
→ C:\01 클로드코드\40-10-00 홍보 이미지 제작 플랫폼\test-click.html
→ 더블클릭
```

**방법 B: 메인 애플리케이션 테스트**
```
파일 탐색기 열기
→ C:\01 클로드코드\40-10-00 홍보 이미지 제작 플랫폼\index.html
→ 더블클릭
```

### 2️⃣ 개발자 도구 열기

브라우저 열린 후:
- **Windows**: `F12` 또는 `Ctrl + Shift + I`
- **Mac**: `Cmd + Option + I`

### 3️⃣ 콘솔 탭 확인

- **Console** 탭 선택
- 다음 메시지가 보이면 앱이 정상 로드된 것입니다:

```
🚀 DOMContentLoaded 이벤트 발생
🏗️ APIClient 생성 중...
✅ APIClient 생성 완료
🎨 UIManager 생성 중...
✅ UIManager 생성 완료
⚙️ App 초기화 중...
✅ 앱 초기화 완료!
```

## 테스트 단계

### test-click.html (간단한 테스트)

1. 파일 열기
2. 콘솔에서 다음 메시지 확인:
   ```
   테스트 페이지 로드됨
   4개 카드 생성됨
   ✅ 이벤트 델리게이션 설정 완료
   👉 아래 카드를 클릭해보세요
   ```
3. 아래의 4개 카드 중 하나 클릭
4. **예상 결과**: 콘솔과 화면에 다음과 같은 메시지 나타남
   ```
   ✅ 카드 클릭 감지: 기술/IT (ID: 1)
   ```
   그리고 카드의 배경색이 파란색으로 변함

### index.html (메인 애플리케이션)

1. 파일 열기
2. 콘솔에서 초기화 메시지 확인
3. 페이지에서 "📌 산업분야를 선택해주세요" 섹션 확인
4. 15개의 테마 카드가 보이는지 확인
5. 카드 중 하나 클릭 (예: 💻 기술/IT)
6. **예상 결과**:
   - 콘솔: `✅ 델리게이션 방식으로 카드 클릭 감지: 기술/IT`
   - 화면: 선택된 카드가 파란색으로 변하고, 아래에 Step 2 로고 업로드 영역이 나타남

## 문제 해결

### 문제: 카드를 클릭해도 아무 반응이 없음

**확인 사항:**

1. **콘솔 에러 확인**
   ```
   F12 → Console 탭
   빨간색 에러 메시지가 있는지 확인
   ```

2. **네트워크 오류 확인**
   ```
   F12 → Network 탭 → 새로고침 (Ctrl+R)
   빨간색 파일이 있는지 확인 (로드 실패한 파일)
   ```

3. **JavaScript 파일 로드 확인**
   ```
   F12 → Console 탭에서 다음 명령 실행:
   CONFIG  // 설정이 로드되었는지 확인
   ```

4. **카드 요소 확인**
   ```
   F12 → Console 탭에서 다음 명령 실행:
   document.querySelectorAll('.theme-selection-card').length
   // 15가 출력되어야 함
   ```

### 문제: 네트워크 탭에서 css/style.css가 빨간색 (404)

이 경우, CSS가 로드되지 않아 카드가 보이지 않을 수 있습니다.

**해결:**
```
1. 파일 탐색기에서 확인: C:\01 클로드코드\40-10-00 홍보 이미지 제작 플랫폼\css\style.css 존재?
2. 있다면 → 브라우저 캐시 삭제 (Ctrl+Shift+Delete) → 새로고침
```

### 문제: 콘솔에 "CONFIG is not defined" 에러

**이유**: config.js가 로드되지 않음

**해결:**
```
1. 개발자도구 → Network 탭 확인
2. js/config.js가 빨간색(404)이면 파일 경로 확인
3. 브라우저 새로고침
```

## 디버깅 팁

### 콘솔에서 직접 테스트

```javascript
// 카드 요소 개수 확인
document.querySelectorAll('.theme-selection-card').length

// 첫 번째 카드 클릭 시뮬레이션
document.querySelector('.theme-selection-card').click()

// 현재 선택된 테마 확인
window.ui?.currentThemeId

// 컨테이너가 클릭 이벤트를 받고 있는지 확인
document.getElementById('theme-selection').addEventListener('click', () => console.log('🎯 클릭됨'))
```

## 성공 체크리스트

- [ ] 페이지 열기
- [ ] 콘솔에 초기화 메시지 표시
- [ ] 15개 테마 카드가 보임
- [ ] 카드 클릭 시 "델리게이션 방식으로 카드 클릭 감지" 메시지 나타남
- [ ] 선택된 카드가 파란색으로 변함
- [ ] 선택된 카드 아래에 Step 2 영역이 나타남

## 다음 단계

카드 클릭이 작동하면:
1. 로고 업로드 (Step 2)
2. 프롬프트 입력 (Step 3)
3. 이미지 생성 버튼 클릭
4. 생성된 이미지 확인

---

**문제가 지속되면**: 
- 브라우저 새로고침: `Ctrl + R`
- 캐시 완전 삭제: `Ctrl + Shift + Delete`
- 다른 브라우저 시도 (Chrome, Firefox, Edge)
