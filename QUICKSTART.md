# 🚀 빠른 시작 가이드

## 1️⃣ 가장 간단한 방법

### Windows 사용자
```
1. run.bat 파일을 더블클릭
2. 자동으로 브라우저가 열림
3. 테마를 선택하고 로고를 업로드한 후 이미지 생성!
```

### Mac/Linux 사용자
```bash
# 터미널에서 실행
chmod +x run.sh
./run.sh
```

---

## 2️⃣ 직접 파일로 열기 (서버 없음)

### Windows
```
1. 폴더에서 index.html 마우스 우클릭
2. "프로그램으로 열기" → Chrome/Edge/Safari 선택
3. 테마 선택 → 로고 업로드 → 이미지 생성!
```

### Mac
```
1. Finder에서 index.html 찾기
2. 오른쪽 클릭 → "다음으로 열기" → Safari/Chrome 선택
3. 사용 시작!
```

### Linux
```bash
# 브라우저로 직접 파일 열기
xdg-open index.html
```

---

## 3️⃣ 수동으로 웹 서버 실행

### Python 설치되어 있는 경우
```bash
python -m http.server 8888
# 또는
python3 -m http.server 8888
```

### Node.js 설치되어 있는 경우
```bash
npx http-server -p 8888
```

브라우저에서 `http://localhost:8888` 열기

---

## ✨ 기능

### 현재 (테스트 모드)
- ✅ 15가지 산업분야 테마 선택
- ✅ 로고 업로드
- ✅ 이미지 생성 (샘플)
- ✅ 로고 자동 삽입
- ✅ 이미지 다운로드
- ✅ 사용 가이드 보기

### API 키 추가 시
- 🔑 실제 OpenAI GPT Image2 (DALL-E 3) 사용
- 🔑 Supabase 데이터베이스 연동
- 🔑 생성 이력 저장

---

## 📋 시스템 요구사항

| 항목 | 요구사항 |
|------|---------|
| **브라우저** | Chrome, Safari, Edge, Firefox (최신 버전) |
| **서버** | Python 또는 Node.js (선택사항) |
| **API 키** | 없어도 테스트 모드로 작동 |
| **화면 크기** | 모바일, 태블릿, 데스크톱 모두 지원 |

---

## 🎯 사용 방법

### 1단계: 테마 선택
```
15가지 산업분야 중 원하는 테마를 카드로 선택
예: 기술/IT, 금융, 의료, 교육, 부동산 등
```

### 2단계: 로고 업로드
```
회사 또는 브랜드 로고 이미지 선택 (JPG, PNG, WebP, GIF)
로고는 생성된 이미지의 왼쪽 상단에 자동으로 삽입됨
```

### 3단계: 주제 입력
```
생성할 이미지의 상세한 설명을 입력
예: "클라우드 기술을 표현하는 현대적인 비즈니스 이미지"
```

### 4단계: 이미지 생성
```
✨ 이미지 생성 버튼 클릭
테스트 모드: 1.5초 후 샘플 이미지 표시
실제 API: 30초~1분 소요
```

### 5단계: 다운로드 또는 공유
```
⬇️ 다운로드: 이미지를 파일로 저장
🔗 URL 복사: 링크를 다른 곳에 붙여넣기
```

---

## ❓ 자주 묻는 질문

**Q: API 키 없이도 작동하나요?**
A: 네! 테스트 모드에서는 API 키 없이 샘플 이미지를 생성합니다.

**Q: 로컬에서 안 열려요.**
A: run.bat (Windows) 또는 run.sh (Mac/Linux)를 실행하거나, Python/Node.js로 웹 서버를 띄워보세요.

**Q: 실제 이미지를 생성하려면?**
A: `.env.example`을 `.env`로 복사하고 API 키를 추가한 후, `js/config.js`에서 `TEST_MODE: false`로 변경하세요.

**Q: 모바일에서도 작동하나요?**
A: 네! 완벽하게 반응형으로 설계되었습니다. 모바일 브라우저에서도 모든 기능이 작동합니다.

---

## 📧 지원

문제가 발생하면:
1. 브라우저 개발자 도구 (F12) → 콘솔 탭 확인
2. README.md의 FAQ 섹션 확인
3. manual.html의 사용자 매뉴얼 참고

---

**즐거운 이미지 생성 되세요! 🎨✨**
